instances: restart simulation with different amount of ants
record time: for optimization
why is it faster when ants spread, but slow in beginning?
bug: ants disappear over time
bug: fps spikes, when I started using `#[wasm_bindgen]` instead of `serde_wasm_bindgen::to_value`
  - weird because this should actually speed up data transfer
